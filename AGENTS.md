# Agent Guardrails (Read first)

- Never run git init outside this repo.
- Always confirm `pwd` is this repo before any git/npm commands:
  /mnt/c/Users/salma/OneDrive/Desktop/ostr-fashion-frontend
- Never use `--force` push unless explicitly told.
- Never copy files from /mnt/c/Users/salma/ root.
- Follow: plan.md, skill.md, memory.md.
